# 测试环境配置（进 git，保证全新 clone 上 mvn package 可通过）
# TODO(Step 1.1 移除)：阶段 0 尚无数据库配置，排除 DataSource 自动装配，理由见 application.yml 注释
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
