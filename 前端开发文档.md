# 今天吃什么 · 前端开发文档

> 依据《开发文档.md》阶段 F：后端全部完成后，Vue3 前端与后端功能对接。
> 前端代码位于 `frontend/`（整体加入后端仓库 .gitignore，不上传 GitHub，部署/仓库策略另行管理）。

## 0. 协作规则

与后端一致：按页面 Step 顺序实现，每页完成自测通过后更新进度记录并 commit。

## 1. 技术选型

| 项 | 选择 | 说明 |
|----|------|------|
| 框架 | Vue 3（Composition API，`<script setup>`） | SFC 单文件组件 |
| 构建 | Vite 5 | dev 代理 `/api` → `http://localhost:18080` |
| 路由 | vue-router 4 | 登录守卫：无 token 跳 /login |
| 状态 | 轻量组合式 store（`src/stores/auth.js`） | token + 用户信息，localStorage 持久化 |
| HTTP | 原生 fetch 封装（`src/api/http.js`） | 统一携带 Authorization、解析 Result、401 跳登录 |
| SSE | fetch + ReadableStream 手工解析 | EventSource 不支持 POST/自定义 Header |

## 2. 页面拆解（4 个页面）

### F.1 登录页（/login）
- **对接**：`POST /api/auth/register`、`POST /api/auth/login`（存 token/用户信息）、`GET /api/auth/me`
- **功能**：登录/注册切换表单；校验用户名 4-20 位、密码 ≥6 位；登录成功跳 `/chat`
- **验收**：注册→自动登录→进入聊天页；错误提示展示后端 message

### F.2 菜谱页（/recipes）
- **对接**：`GET /api/documents`（分页+keyword）、`GET /api/documents/{id}`（详情原文，markdown 纯文本渲染）
- **功能**：搜索框 + 卡片列表（标题/分类/难度★/卡路里）+ 分页；点卡片打开详情抽屉（原文滚动查看）
- **验收**：keyword=红烧鱼 命中 2 条；详情展示原文全文

### F.3 聊天页（/chat，核心）
- **对接**：`POST /api/conversations`（新建）、`GET /api/conversations`（列表）、`DELETE /api/conversations/{id}`、`GET /api/conversations/{id}/messages`（历史）、`POST /api/conversations/{id}/chat`（SSE）
- **SSE 消费**：fetch POST（`Accept: text/event-stream`）→ ReadableStream 按 `\n\n` 分帧 → 解析 `event:`/`data:`；按 §5.6 契约渲染：
  - `AGENT_START`：创建"思考中"占位
  - `TOOL_START` / `TOOL_RESULT`：执行过程时间线（工具名/耗时/摘要）
  - `ANSWER_DELTA`：增量追加到回答区
  - `REFERENCE`：引用卡片（标题/章节/片段/完整菜谱标记），点击调 `GET /api/documents/{id}` 查看原文
  - `DONE`：结束态；`ERROR`：错误提示
- **验收**：完整一轮对话渲染时间线/回答/引用；历史会话可回看

### F.4 管理页（/admin，ADMIN 可见）
- **对接**：`GET/POST /api/admin/documents`、`POST batch-import`、`POST {id}/reindex`、`DELETE {id}`、`GET /api/admin/index-tasks`、`POST index-tasks/{taskNo}/retry`、`GET /api/admin/audit-logs`
- **功能**：三个 Tab——①文档（列表/上传 md/批量导入/重索引/删除）②任务（状态筛选/重试）③审计日志（分页）
- **验收**：上传后任务出现；retry 将 FAILED 置 PENDING；审计可见 RETRY_INDEX_TASK

## 3. 工程结构

```
frontend/
├── package.json / vite.config.js / index.html
└── src/
    ├── main.js / App.vue / router.js
    ├── api/          # http.js（fetch 封装）、各资源 api
    ├── stores/       # auth.js
    └── views/        # LoginView / RecipesView / ChatView / AdminView
```

## 4. 建议顺序

F.1 → F.2 → F.3 → F.4 → 整体联调（`npm run build` 通过 + dev 起服务四页可访问）。
