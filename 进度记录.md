# 今天吃什么 · 进度记录

> 本文档与《今天吃什么-开发文档.md》配套使用，是 **AI 开发过程的唯一进度台账**，供用户 review。

---

## 使用规则（AI 必读，每步照做）

1. **AI 每完成一个 Step**（自测通过后、停下等待 review 前），必须在本文件「步骤记录」对应小节填写记录，格式见模板。
2. 填完后将「状态总览表」中该 Step 状态改为 **待 Review**，并在对话中提示用户 review。
3. 用户 review 结论回填到该 Step 记录的「Review 结论」字段，状态改为 **已通过**（或 **返工中**）。
4. 用户确认后才执行 `git commit` + `git push`，commit hash 回填记录。**commit 必须在 review 通过之后，顺序不可颠倒。**
5. 「偏离设计的决策」「遗留问题」必须如实写，不许粉饰——这是 review 时最该看的地方。
6. 状态取值：`未开始 / 进行中 / 待 Review / 返工中 / 已通过`。

---

## 状态总览表

| Step | 内容 | 状态 | Review 结论 | Commit |
|------|------|------|-------------|--------|
| 0.1 | 父子 POM 改造与版本降级 | 待 Review | — | — |
| 0.2 | docker-compose 基础设施 | 未开始 | — | — |
| 0.3 | 主应用基础配置与统一规范 | 未开始 | — | — |
| 1.1 | 数据库 DDL 与实体 Mapper 层 | 未开始 | — | — |
| 1.2 | 认证与 RBAC（Sa-Token） | 未开始 | — | — |
| 1.3 | 审计日志（AOP） | 未开始 | — | — |
| 1.4 | 用户偏好接口 | 未开始 | — | — |
| 2.1 | RustFS 存储服务 | 未开始 | — | — |
| 2.2 | 文档管理 API（管理端） | 未开始 | — | — |
| 2.3 | RocketMQ 异步索引骨架 | 未开始 | — | — |
| 2.4 | Markdown 解析与分块 | 未开始 | — | — |
| 2.5 | Embedding 与索引服务闭环 | 未开始 | — | — |
| 3.1 | 混合检索服务 | 未开始 | — | — |
| 3.2 | MCP Tool Server | 未开始 | — | — |
| 3.3 | 主应用 MCP 客户端接入 | 未开始 | — | — |
| 4.1 | 会话与消息持久化 | 未开始 | — | — |
| 4.2 | Agent 服务（核心） | 未开始 | — | — |
| 4.3 | SSE 流式接口 | 未开始 | — | — |
| 4.4 | 对话限流与稳定性 | 未开始 | — | — |
| 5.1 | 管理端任务与审计接口 | 未开始 | — | — |
| 5.2 | 全链路集成验收 | 未开始 | — | — |
| F | 前端 Vue3（后置，另出文档） | 未开始 | — | — |

---

## 步骤记录

> 每条记录模板（AI 按此填写，字段不可省略，无内容写"无"）：

```
### Step X.Y：〈步骤名〉
- **完成时间**：YYYY-MM-DD HH:mm
- **状态**：待 Review
- **交付清单**：〈新增/修改的文件路径列表，标注关键文件作用〉
- **主要实现说明**：〈3-6 句，说清做了什么、关键类/方法怎么分工〉
- **偏离设计的决策**：〈与开发文档不一致之处及原因；无则写"无"〉
- **自测结果**：〈执行了哪些验证、命令/步骤、结果；失败项如何处理〉
- **遗留问题**：〈已知未解决项及影响；无则写"无"〉
- **Review 结论**：〈用户填写：通过 / 不通过 + 意见〉
- **Commit**：〈通过后填写 commit hash 与 message〉
```

---

### Step 0.1：父子 POM 改造与版本降级
- **完成时间**：2026-09-16 21:28
- **状态**：待 Review
- **交付清单**：
  - `backend/pom.xml`（重写）：父 POM，packaging=pom，parent=spring-boot-starter-parent **3.5.16**，modules 指向三子模块，dependencyManagement 统一管理 Spring AI BOM / AWS SDK BOM / Sa-Token / MyBatis-Plus / pgvector / RocketMQ / commonmark / 内部模块版本
  - `backend/howtocook-common/pom.xml`（新增）：公共库（纯 jar），引 MyBatis-Plus starter、data-redis、pgvector、AWS S3 SDK v2、commonmark、Lombok
  - `backend/howtocook-app/pom.xml`（新增）：主应用，引 common + web/validation/actuator、Sa-Token（starter+redis-jackson）、spring-security-crypto（仅 BCrypt）、Flyway（core+postgresql）、postgresql 驱动、RocketMQ starter、spring-ai-starter-mcp-client、spring-boot-maven-plugin
  - `backend/howtocook-mcp-server/pom.xml`（新增）：MCP Server，引 common + spring-ai-starter-mcp-server-webmvc + postgresql + spring-boot-maven-plugin
  - `backend/howtocook-app/src/main/java/com/anfioo/howtocook/app/HowToCookApplication.java`（新增，沿用原模板主类改名迁移）
  - `backend/howtocook-app/src/test/java/com/anfioo/howtocook/app/HowToCookApplicationTests.java`（新增，contextLoads）
  - `backend/howtocook-app/src/test/resources/application.properties`（新增，见偏离决策②）
  - `backend/howtocook-app/src/main/resources/application-example.yml`（自原 src 迁移，§4.2 配置模板原样保留）
  - `backend/howtocook-app/src/main/resources/application.yml`（本地真实配置，已 gitignore，目前仅应用名+临时排除项）
  - `backend/howtocook-mcp-server/src/main/resources/application-example.yml` + `application.yml`（占位，Step 3.2 敲定完整配置）
  - `.gitignore`（项目根新增：target/、.idea/、两个模块的 application.yml、backend/docker/.env、日志等）
  - 删除：原 `backend/src/`（单模块代码）、`backend/HELP.md`（IDEA 模板遗留）
- **主要实现说明**：backend 从 Boot 4.0.8 单模块改造为「父 POM + common/app/mcp-server 三模块」，依赖方向 app→common、mcp-server→common 无环。所有版本以 Maven Central repo1 元数据为准逐一核实后写入父 POM properties。主类迁移至 com.anfioo.howtocook.app 包并更名 HowToCookApplication。
- **偏离设计的决策**：
  1. **Spring AI 1.1.x 无 `spring-ai-starter-mcp-client-webmvc` 工件**（Maven Central 不存在，已核实），主应用改用 `spring-ai-starter-mcp-client`（JDK HttpClient Streamable HTTP 传输，WebMVC 栈可用，功能等价），Step 3.3 接连接配置时再验证；
  2. 阶段 0 无数据源，classpath 上 JDBC 驱动 + MyBatis-Plus starter 触发 DataSourceAutoConfiguration 启动失败（"Failed to determine a suitable driver class"），在 app 本地 application.yml 与测试资源 application.properties 中排除 `DataSourceAutoConfiguration` 并 TODO 标记，**Step 1.1 配置真实数据源时移除**（沿用开发文档 Step 0.3 的注释 TODO 约定，属提前使用）；
  3. spring-ai openai model starter（ChatClient/EmbeddingModel 所需）未在本步引入——空配置缺 api-key 会导致启动失败，按需在 Step 2.5 / 4.2 引入；
  4. mcp-server 模块本步附带 application-example.yml 占位。
- **自测结果**：
  1. `mvn clean package`（Maven 3.9.16 / JDK 17）：**三模块全部 SUCCESS**，app 模块 contextLoads 测试通过；
  2. 启动验证：`java -jar howtocook-app-0.0.1-SNAPSHOT.jar --server.port=18080` 启动成功（Started HowToCookApplication in 10.47s），`/actuator/health` 返回 503 DOWN——Redis 健康指示器所致，Redis 属 Step 0.2 基础设施，阶段 0 属预期；kill 后进程正常退出；
  3. 版本核实：Boot 3.5.16 / Spring AI BOM 1.1.8 / sa-token 1.46.0 / mybatis-plus 3.5.17 / pgvector 0.1.6 / rocketmq-spring 2.3.6 / commonmark 0.24.0 / aws-sdk-bom 2.54.19，均经 repo1.maven.org maven-metadata 确认存在且为最新稳定 patch。
- **遗留问题**：
  1. 本开发终端（Git Bash 沙箱）因环境变量 `MSYS_NO_PATHCONV=1` 导致 mvnw 脚本传给 java.exe 的 classpath 为 Unix 风格路径而无法启动 Maven，本终端改用直连 java 启动 Maven 的方式构建；**用户在 IDEA 或 PowerShell 中使用 mvnw.cmd 不受影响**，非工程问题；
  2. 本终端存在环境变量 `SERVER__PORT=64645`（relaxed binding 为 server.port 且端口被宿主占用），本终端启动 app 需显式 `--server.port`，同样非工程问题。
- **Review 结论**：（待用户填写）
- **Commit**：（通过后填写）

### Step 0.2：docker-compose 基础设施
（待填写）

### Step 0.3：主应用基础配置与统一规范
（待填写）

### Step 1.1：数据库 DDL 与实体 Mapper 层
（待填写）

### Step 1.2：认证与 RBAC（Sa-Token）
（待填写）

### Step 1.3：审计日志（AOP）
（待填写）

### Step 1.4：用户偏好接口
（待填写）

### Step 2.1：RustFS 存储服务
（待填写）

### Step 2.2：文档管理 API（管理端）
（待填写）

### Step 2.3：RocketMQ 异步索引骨架
（待填写）

### Step 2.4：Markdown 解析与分块
（待填写）

### Step 2.5：Embedding 与索引服务闭环
（待填写）

### Step 3.1：混合检索服务
（待填写）

### Step 3.2：MCP Tool Server
（待填写）

### Step 3.3：主应用 MCP 客户端接入
（待填写）

### Step 4.1：会话与消息持久化
（待填写）

### Step 4.2：Agent 服务（核心）
（待填写）

### Step 4.3：SSE 流式接口
（待填写）

### Step 4.4：对话限流与稳定性
（待填写）

### Step 5.1：管理端任务与审计接口
（待填写）

### Step 5.2：全链路集成验收
（待填写）

### Step F：前端 Vue3
（后置，待后端完成后另出前端开发文档，此处仅登记里程碑）

---

## 版本与决策台账

> 凡实现过程中确定的依赖版本、技术决策（含开发文档 §8 风险预案的触发与执行），逐条登记：

| 日期 | 决策/版本 | 原因 | 登记人 |
|------|-----------|------|--------|
| 2026-09-16 | 认证选型落定 Sa-Token | 用户决策，轻量优先 | — |
| 2026-09-16 | 工程结构落定 Maven 多模块（common/app/mcp-server） | 用户决策，MCP 独立部署且底层能力复用 | — |
| 2026-09-16 | 版本基线：Boot 3.5.16 / Spring AI BOM 1.1.8 / Sa-Token 1.46.0 / MyBatis-Plus 3.5.17 / pgvector 0.1.6 / RocketMQ-Spring 2.3.6 / commonmark 0.24.0 / AWS SDK BOM 2.54.19 | 均经 repo1.maven.org 元数据核实为与 Boot 3.5 兼容的最新稳定版 | AI |
| 2026-09-16 | 主应用 MCP 客户端使用 `spring-ai-starter-mcp-client`（非 webmvc 变体） | Spring AI 1.1.x 不存在 mcp-client-webmvc 工件（已核实 Maven Central），JDK HttpClient 传输在 WebMVC 栈可用 | AI |
| 2026-09-16 | 阶段 0 临时排除 DataSourceAutoConfiguration（TODO Step 1.1 移除） | 无数据源时 JDBC 驱动在 classpath 会导致启动失败 | AI |
