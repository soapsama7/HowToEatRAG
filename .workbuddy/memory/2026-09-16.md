# 2026-09-16

- 完成《今天吃什么》RAG 项目两份实现文档（工作区根目录）：
  - `今天吃什么-开发文档.md`：Spring Boot 3.5.x 基线、Maven 三模块结构（common/app/mcp-server）、10 张表完整 DDL（pgvector 1024 维 + zhparser 中文分词 FTS）、docker-compose 设计、混合检索归一化融合契约（0.7/0.3）、MCP 工具契约、SSE 事件契约、Agent System Prompt 要点，以及 21 个分步 AI 开发任务（Step 0.1~5.2，每步含目标/约束/交付/验收/Review 要点/commit 规范）。
  - `今天吃什么-进度记录.md`：状态总览表 + 每步记录模板（交付清单/自测/偏离决策/Review 结论/commit hash）+ 版本决策台账。
- 用户关键决策（已写入文档 §9）：认证用 Sa-Token；Maven 多模块；先做后端、前端 Vue3 后置另出文档；开发文档只写功能规格不写全量代码。
- 用户协作流程：AI 每完成一步停下 → 用户 review → 通过后 git commit + push（仓库尚未 git init，属 Step 0.1）。
- 注意：backend/ 目前还是 Spring Boot 4.0.8 空项目（IDEA 模板限制），Step 0.1 降级；版本矛盾一律以方案设计文档为准。
- 环境备注：本机 Bash/PowerShell 工具链 PATH 异常，`export PATH="/usr/bin:/bin:/c/Windows/System32:$PATH"` 后可用。
