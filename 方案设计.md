# 今天吃什么 · Agentic RAG 个人饮食助手 — 方案设计

> 本文档为**最终敲定方案**,只描述技术选型、架构设计与功能边界,不含具体实现细节(实现细节后续在各模块开发文档中补充)。
>
> 项目定位:以开源菜谱 **HowToCook** 为知识源,基于 **Spring Boot + Spring AI** 构建的 **Agentic RAG** 个人饮食助手。核心特征是**由 LLM 自主决定是否检索知识库**,而非在代码中写死"每次必检索"。

---

## 一、项目定位

- **一句话**:用户报出食材 / 口味 / 偏好,Agent 判断是否需要查菜谱知识库,结合检索结果与用户画像生成个性化推荐,并展示参考资料与执行过程。
- **架构范式**:Agentic RAG —— `用户 → Agent → LLM 决策 → (按需)工具调用 → 综合回答 + 引用溯源`。
- **规模取舍**:做**简化版**,聚焦跑通「文档入库 → 混合检索 → Agent 决策 → 回答 + 引用」核心链路,并完整覆盖用户、权限、文档管理、会话持久化、审计日志等后端工程能力。
- **知识来源**:HowToCook(菜谱、食材、烹饪步骤、烹饪技巧、食品安全、厨房准备等)。

---

## 二、最终技术选型

| 分类 | 选型 | 说明 |
|------|------|------|
| 语言 / 基础框架 | Java 17、Spring Boot 3.x、Spring Web MVC | 单体应用,不拆微服务 |
| AI 框架 | Spring AI(ChatClient / ChatModel / EmbeddingModel) + MCP 协议 | Agent 决策 + RAG 能力核心,工具通过 MCP 协议调用 |
| 模型服务商 | 火山方舟(聊天,doubao-seed-evolving) + 阿里云百炼(向量,qwen3.7-text-embedding,1024维) | 免费模型,兼容 OpenAI 协议;chat 与 embedding 分别使用不同 API Key |
| 认证鉴权 | Sa-Token或Spring Security（学一下） | RBAC + Token 会话,区分 USER / ADMIN |
| 持久层 | MyBatis-Plus | ORM |
| 数据库 | PostgreSQL + pgvector | 一库承担:业务数据 + 全文检索(FTS) + 向量检索(1024维) |
| 对象存储 | RustFS(S3 兼容) | 存原始 markdown 文件本体,数据库只存元数据与 Object Key(索引) |
| 文档解析 | v1 直接读取 markdown 纯文本(可用 commonmark-java 等轻量库提取标题/元数据) | markdown 是纯文本,无需 Tika;PDF/DOCX 等二进制格式归入后续增强 |
| 缓存 / 异步 | Redis | 保存登录状态、对话限流、缓存热点菜谱等 |
| 校验 / 工具 | Bean Validation、Lombok | 参数校验、样板代码 |
| 接口交互 | REST + SSE | 对话流式输出 |
| 前端 | Vue3 | 登录、菜谱、聊天、管理后台 |
| 部署 | Docker Compose | 第三方服务全部容器化 |
| 消息中间件 | RocketMQ | 管理员上传文档时经由消息中间件异步触发索引任务 |
| MCP 工具服务 | 独立部署的 MCP Tool Server | 提供 search_chunks / get_recipe_detail 等工具,主应用通过 MCP 协议调用 |

---

## 三、总体架构

```
Frontend (SPA) ──HTTP / SSE──▶ Spring Boot (主应用)
                                  ├── 用户 / 权限 (Sa-Token + RBAC)
                                  ├── 文档(菜谱)管理 / 知识库管理 / 审计日志
                                  ├── 会话持久化 (Conversation / Message)
                                  └── Agent Service ──▶ Spring AI ChatClient ──▶ LLM (火山方舟 doubao)
                                                                   │
                                                                   ▼ MCP 协议
                                                          MCP Tool Server (独立部署)
                                                                   ├── search_chunks(query, topK=5)
                                                                   │      └──▶ Hybrid Retriever
                                                                   │              ├── pgvector (向量)
                                                                   │              └── PostgreSQL FTS (关键词)
                                                                   │              ▼
                                                                   │          Fusion → Top K chunks
                                                                   └── get_recipe_detail(doc_id)
                                                                          ├──▶ PostgreSQL (查 object_key)
                                                                          └──▶ RustFS (取原始 markdown)
                                                                   ▼
                                                     Agent Answer ──▶ 最终回答 + 引用溯源
```

**模块分层**:

- **接入层**:REST API + SSE。
- **业务层**:用户权限、文档(菜谱)管理、知识库管理、会话、审计日志。
- **Agent 层**:ChatClient 编排、MCP 协议调用、执行过程追踪。
- **能力层**:MCP Tool Server(独立部署),提供 search_chunks(混合检索返回 chunk)、get_recipe_detail(按 doc_id 取完整菜谱) 等工具。
- **存储层**:PostgreSQL(业务 + pgvector + FTS)、RustFS(原始 markdown 文件本体)、Redis(可选)。

---

## 四、核心链路设计

### 4.1 文档入库链路

```
管理员上传 markdown → 存原始文件到 RustFS → 创建 Document 记录(含 object_key, status=PENDING)
   → 发送 RocketMQ 消息 → 异步消费(status=INDEXING):
       读取 markdown 纯文本 → 文本清洗 → 切分(Chunk) → Embedding
       → 写入 document_chunk 表(embedding + doc_id) + FTS 索引 → status=READY(可检索)
   失败则 status=FAILED
```

- 上传接口**立即返回 taskId**,索引过程通过 RocketMQ 异步执行。
- **文档状态流转**:`PENDING`(已上传待索引) → `INDEXING`(索引中) → `READY`(可检索) / `FAILED`(索引失败)。**Agent 检索时仅返回 status=READY 的文档对应的 chunk**,防止搜到不可用文档。
- **存储职责划分**:原始 markdown 文件本体存 RustFS(通过 object_key 引用);文档元数据(title/type/object_key/难度/时间/菜系/status 等)存 `document` 表;chunk 内容与 embedding 存 `document_chunk` 表。

### 4.2 混合检索链路

采用**向量检索 + 关键词检索**并融合,而非单一向量检索;**检索单位是 chunk**(不是整个菜谱):

```
query ──▶ Embedding ──▶ pgvector 相似度检索 ─┐
      └─▶ 文本处理 ──▶ PostgreSQL FTS 关键词 ─┴─▶ 归一化 → 去重 → 加权融合 → Top K chunks (默认5)
```

- **返回结果**:Top K 个 chunk(默认 K=5),每个 chunk 含 `doc_id / chunk_id / content / score / retrievalType`。
- **融合策略(本期敲定)**:加权融合,`finalScore = 0.7 * vectorScore_norm + 0.3 * keywordScore_norm`。
  - **归一化(必须)**:向量检索的余弦相似度范围 `[0,1]`,而 PostgreSQL FTS 的 `ts_rank` 范围不固定(可能 `[0, 10+]`),度量不统一时权重失去意义。**融合前必须对两路分数各自做 Min-Max 归一化** `score' = (score - min) / (max - min)`,映射到 `[0,1]` 后再加权。
  - 仅命中单路的数据:该路分数归一化后参与计算,另一路给予默认分数 0。
- **其他融合算法对比**(本期不做,了解即可):
  - **RRF(Reciprocal Rank Fusion)**:`finalScore = Σ 1/(k + rank_i)`,k 通常取 60。只看排名不看分数,天然避免归一化问题,对异常分数鲁棒;但丢失分数绝对信息。
  - **Rerank(Cross-Encoder 重排序)**:先召回 Top N(如 N=20),再用 Cross-Encoder 模型对每个 query-chunk 对逐一打分取 Top K。效果最好但延迟最高。
  - **Learning-to-Rank**:用机器学习模型学习最优融合权重,需标注数据,过于复杂。
- RRF、Rerank 归入**后续增强**,本期不做。

### 4.3 Agent 决策链路(两阶段检索)

```
用户问题 → Agent → LLM 判断
   ├── 不需要知识库 → 直接回答
   └── 需要知识库 → 两阶段检索:

        【阶段1: chunk 检索】
        调用 MCP: search_chunks(query, topK=5)
           → 混合检索(pgvector + FTS) → 融合 → Top K chunks
           → 返回 chunk 列表(doc_id / chunk_id / content / score / retrievalType)
           ↓
        LLM 判断 chunk 是否足够回答用户问题
           ├── 足够 → 直接生成回答 + 引用(chunk 级别)
           └── 不够(需要完整菜谱上下文) → 进入阶段2

        【阶段2: 完整菜谱获取(按需)】
        调用 MCP: get_recipe_detail(doc_id)
           → PostgreSQL 查 document 表(通过 doc_id 拿 object_key)
           → RustFS 取原始 markdown(通过 object_key)
           → 返回完整菜谱内容
           ↓
        LLM 综合 chunk + 完整菜谱 → 最终回答 + 引用
```

- **System Prompt** 引导 LLM:
  1. 自主判断是否需要检索知识库,不写死"每次必查"
  2. 拿到 chunk 后,判断是否足够回答;若信息不完整(如缺少具体步骤、用量、注意事项),再调用 `get_recipe_detail` 取完整菜谱
  3. 知识库无足够信息时不得伪造
- **本期 MCP Tool 清单**(独立部署为 MCP Server,主应用通过 MCP 协议调用):
  - `search_chunks(query, topK=5)`:混合检索,返回 chunk 列表(含 `doc_id / chunk_id / content / score / retrievalType`)。
  - `get_recipe_detail(doc_id)`:通过 doc_id 查数据库元数据(object_key),再从 RustFS 取完整 markdown 菜谱。
- **职责边界**:LLM 负责"决定要不要查、查了够不够",Java 后端负责"真正怎么查",MCP Server 负责"工具实现"。
- **doc_id 语义贯穿**:chunk 中存的 doc_id → 数据库 document 表查 object_key → RustFS 取原始 markdown,三跳定位完整菜谱。

### 4.4 会话与流式交互

- **持久化**:所有对话落 `conversation` / `message` 两表,重启后仍可见。
- **消息角色**:`USER / ASSISTANT / TOOL / SYSTEM`。
- **消息类型**(供前端渲染):`USER_MESSAGE / AGENT_THINKING / TOOL_CALL / TOOL_RESULT / REFERENCE / FINAL_ANSWER`。两阶段检索会产生多次 `TOOL_CALL / TOOL_RESULT`(阶段1 的 search_chunks + 阶段2 按需的 get_recipe_detail)。
- **SSE 事件流**:`AGENT_START → TOOL_START → TOOL_RESULT → (可选)TOOL_START → TOOL_RESULT → REFERENCE → ANSWER_DELTA(多次) → DONE`。
- **执行过程展示**:页面展示结构化 **Agent Execution Trace**(执行步骤),**不暴露模型真实 Chain of Thought**。
- **引用溯源**:后端每条引用保留 `docId / chunkId / title / content(chunk 片段) / score / retrievalType`,`retrievalType ∈ {VECTOR, KEYWORD, HYBRID}`。若阶段2 取了完整菜谱,引用可额外标记 `fullRecipeFetched=true`。
- **前端展示规则(重要)**:前端**只展示** LLM 的回答、引用资料的标题与内容片段等必备信息;**不得展示** `score`(相似度分数)、`retrievalType`(检索类型)等后端调试信息。这些字段仅供后端日志/调试使用,前端接口返回时应过滤掉。

---

## 五、数据模型

> 仅列出表清单与分组,字段级设计后续补充。

| 分组 | 表 |
|------|----|
| 用户权限 | `user`、`role`、`user_role`、`user_preference` |
| 知识库 | `document`(菜谱元数据:id/title/object_key/难度/时间/菜系/type/version/status 等)、`document_chunk`(含 `doc_id` 外键、`content` 片段、`embedding` 向量(1024维)、`version` 版本一致性字段) |
| 会话 | `conversation`、`message` |
| 运维 | `audit_log`、`index_task` |

- **用户偏好**:第一版作为结构化数据存 `user_preference`(辣度、饮食类型、过敏原、忌口/喜好食材、健康目标),Agent 请求时读取注入。
- **版本一致性**:`document.version` 与 `document_chunk.version` 比对,判断知识库是否需重新索引。
- **菜谱本体存储**:原始 markdown 文件存 RustFS,数据库只存元数据与 object_key(索引);需要完整菜谱时,通过 `doc_id → object_key → RustFS` 三跳取原文。
- **文档状态字段**:`document.status` 取值 `PENDING / INDEXING / READY / FAILED`,Agent 检索时仅返回 status=READY 的文档对应的 chunk,防止搜到不可用文档。
- **数据模型简化说明**:本期删除 `recipe`、`recipe_ingredient`、`recipe_step` 三张表,菜谱的结构化字段(难度/时间/菜系)合并到 `document` 表,详细食材/步骤/注意事项从 RustFS 原始 markdown 中取。

---

## 六、功能范围

### 6.1 本期实现(MVP)

- **基础后端**:用户注册/登录、RBAC、文档(菜谱) CRUD、用户偏好、对话持久化。
- **RAG**:Markdown 上传、RustFS 对象存储、markdown 纯文本读取、Chunk、Embedding、pgvector、PostgreSQL FTS、混合检索(返回 chunk,含归一化)、引用资料。
- **Agent**:ChatClient、MCP 协议调用、MCP Tool Server(独立部署,提供 search_chunks / get_recipe_detail)、两阶段检索(chunk → 按需取完整菜谱)、LLM 自主决定是否检索、Tool 重试/超时、最大执行轮数、Agent 状态机等。
- **用户偏好管理**:通过数据库 `user_preference` 表管理用户个性化需求(辣度、饮食类型、过敏原、忌口/喜好食材、健康目标),Agent 请求时读取注入。**不做"长期记忆"**(概念模糊,直接用数据库结构化存储处理)。
- **前端**:登录、菜谱列表、聊天页面(仅展示回答 + 引用资料,不展示 score/相似度等调试信息 + Agent Trace)、管理后台。
- **工程化**:SSE、审计日志(AOP)、RocketMQ 异步索引任务、统一异常处理、参数校验、事务、乐观锁、Docker Compose。

### 6.2 后续增强(有余力再做)

- **RAG**:RRF、Rerank、Query Rewrite、Multi Query、Metadata Filter。
- **文档解析**:Apache Tika(docker 部署,端口 9998),支持 PDF/DOCX/PPT 等二进制格式(含 OCR、表格提取等复杂场景)。
- **MCP**:Restaurant MCP Server(`search_nearby_restaurants`)。

### 6.3 明确不做(避免过度设计)

Milvus、Elasticsearch、分布式锁、微服务拆分、复杂工作流、知识图谱、模型路由、熔断降级。

---

## 七、安全设计

- **Prompt Injection 防护**:知识库文本一律视为 **knowledge content(数据)**,不得当作系统指令执行。
- **权限隔离**:普通用户不可访问管理端接口(如 `POST /admin/documents`、`DELETE /admin/documents`、`GET /admin/audit`)。
- **文件安全**:限制上传文件类型/大小/文件名,禁止路径穿越(`../../`),RustFS 对象存储使用随机 Object Key。
- **MCP Server 鉴权(重要)**:MCP Tool Server 不直接对外暴露,仅接受来自主应用的内部调用。若直接暴露 `localhost:xxxx/search_chunks`、`/get_recipe_detail`,任何人都能调用,必须做以下安全措施:
  - **网络隔离**:MCP Server 仅在 docker 内网可访问,不映射到宿主机公网端口
  - **内部 Token 鉴权**:主应用调用 MCP Server 时携带预共享的内部 Token(如 `X-Internal-Token` 请求头),MCP Server 校验 Token 有效性后才执行工具函数
  - **接口白名单**:MCP Server 仅暴露 `/search_chunks`、`/get_recipe_detail` 等必要工具接口,不暴露管理端点或健康检查以外的接口

---

## 八、部署方案

```
docker-compose.yml
├── postgres           (含 pgvector 扩展,向量维度 1024)
├── rustfs             (S3 兼容对象存储,存原始 markdown,可替换为 minio)
├── redis
├── rocketmq           (namesrv + broker,文档入库异步消息)
└── mcp-tool-server    (独立部署的 MCP 工具服务,仅 docker 内网可访问,提供 search_chunks / get_recipe_detail)
```

- Spring Boot 主应用本地(IDEA / JAR)运行,开发阶段不强制容器化。
- **环境变量**(chat 与 embedding 分别使用不同 API Key):
  - `LLM_COOK_API_KEY`:火山方舟 chat API Key(doubao-seed-evolving)
  - `LLM_EMBEDDING_API_KEY`:阿里云百炼 embedding API Key(qwen3.7-text-embedding,1024维)

---

## 九、自己补充

### 9.0 大模型配置

RAG项目就这么写，apikey和baseurl别变了，不然又搞半天，这个是稳定能跑的

免费的可能会过期，过期了再换，能用就先用着。

**环境变量说明**(chat 与 embedding 分别使用不同 API Key,不要混用):
- `LLM_COOK_API_KEY`:火山方舟 chat API Key,对应模型 `doubao-seed-evolving`
- `LLM_EMBEDDING_API_KEY`:阿里云百炼 embedding API Key,对应模型 `qwen3.7-text-embedding`
- 向量模型维度是 **1024**,因此 pgvector 也得是 1024 维

```yaml
  ai:
    openai:
      base-url: ${OPENAI_BASE_URL:https://ark.cn-beijing.volces.com/api/v3}
      api-key: ${LLM_COOK_API_KEY}
      embedding:
        model: ${EMBEDDING_MODEL:qwen3.7-text-embedding}
        api-key: ${LLM_EMBEDDING_API_KEY:}
        base-url: https://ws-5z0bdkyytsactui4.cn-beijing.maas.aliyuncs.com/compatible-mode/v1

      chat:
        model: ${CHAT_MODEL:doubao-seed-evolving}
        completions-path: /chat/completions
        temperature: 0.2
```

前面八点是大模型基于我之前的思路随笔概括的，这里写一点我个人的补充，也作为参考。

该项目的RAG链路为：

```
上传文档->解析->分块->向量化->入库->检索->生成
```

根据链路的顺序，依次对其进行一些补充：

### 9.1 文档解析

**v1 仅限 markdown 格式**,markdown 是纯文本,直接读取文件内容即可,不需要 Tika 等重型解析工具。非 markdown 格式直接拒绝。

**存储职责划分**(重要,避免歧义):
- **原始文件本体**存 RustFS(通过 object_key 引用),数据库不存文件内容
- **文档元数据**(title/type/object_key/难度/时间/菜系/status 等)存 `document` 表
- **解析后的文本**切分为 chunk,chunk 内容与 embedding 存 `document_chunk` 表(含 doc_id 外键)

**后续增强**:当需要支持 PDF/DOCX/PPT 等二进制格式时,引入 Apache Tika(docker 部署,端口 9998),参考:[用Apache Tika解析文档 | 拿个offer-AI Agent实战课](https://nageoffer.com/ragent/tika/)。届时需考虑 OCR、表格提取等复杂场景。



### 9.2 向量数据库选择

实际跑了三遍不同选型的demo：

```
PostgreSQL + pgvector
MySQL + Vector
MySQL + Milvus
```

最终选择：`postgresql` + `pgvector`。关系型数据库 + 向量数据库二合一

不用纯`mysql`。即使它在9.x版本已支持向量数据，但是`PostgreSQL`生态更成熟，相关学习资料更多。同时`Spring AI`的适配也更好

不用`MySQL` + `Milvus`。Milvus 更适合把向量检索作为独立基础设施，并且在大规模向量数据和高并发场景下有优势。但此项目预计数据规模比较小，而且还需要用户、文档、权限等关系型数据。使用 PostgreSQL + pgvector 可以统一管理，架构更简单，所以没有为了引入专门的向量数据库而增加系统复杂度。

什么时候可以用`MySQL` + `Milvus`：如果现有业务系统已经以 MySQL 为核心，同时向量数据规模和检索并发增长到需要独立扩展，考虑把业务数据和向量检索拆开，由 MySQL 管业务数据，Milvus 专门负责向量检索



## 附:核心链路一句话总览

```
【入库】markdown → RustFS(原始文件) + RocketMQ(异步) → 纯文本读取 → Chunk → Embedding → document_chunk(pgvector + FTS),status: PENDING→INDEXING→READY
【问答】用户问题 → Agent → MCP: search_chunks(仅 status=READY) → 归一化+向量+关键词融合 → Top K chunks(默认5) → LLM 判断够不够
        ├── 够 → 回答 + 引用(chunk 级别,前端不展示 score)
        └── 不够 → MCP: get_recipe_detail(doc_id) → 数据库查 object_key → RustFS 取完整 markdown → LLM → 回答 + 引用
【用户侧】账号/权限 → 对话持久化 → SSE → Agent Trace(前端仅展示回答+引用资料)
【管理侧】文档管理 → RocketMQ 异步索引任务 → 审计日志
```



## 编码规则

- 不要出现类似于`ChatResponse.Reference`这样的类表示，即一个类里面不要嵌套另一个类的定义，我看的不舒服
- 工具写成MCP服务，不写在原本的项目里边
- `Controller`层里面不要写太多逻辑



## 额外参考资料

来源于：[什么是RAG技术？ | 拿个offer-AI Agent实战课](https://nageoffer.com/ragent/rag/#rag-架构让大模型开卷考试)

---

做好 RAG 并不容易

看完前面的内容，你可能觉得 RAG 原理也不复杂，搭一个 demo 似乎很快。确实，跑通一个仅仅是能用的 RAG 原型，可能一下午就够了。

但是从能用到好用，中间隔着一堆坑。

### 1. 数据入向量库

企业里的知识不会乖乖地以纯文本形式等着你。PDF、Word、PPT、网页、Markdown、数据库，什么都有。光是把这些东西解析成干净的文本，就是一堆脏活累活。PDF 里的表格、扫描件、双栏排版，每一个都是坑。

文档要切成小块才能检索，但切多大是个问题。切太大，检索不精准，一大段里可能只有一两句话有用；切太小，上下文信息丢了，检索到的片段看不懂在说啥。

按段落切？按固定字数切？按语义切？不同类型的文档可能需要不同的策略，没有银弹。

### 2. 问题重写

用户输入的问题，往往不能直接拿去检索。

- **口语化**：用户问“报销咋整”，你拿这四个字去做向量检索，效果能好吗？得把它展开成“公司报销流程是什么，需要准备哪些材料”之类的完整表述，检索效果才会好。
- **多轮对话**：用户第一轮问“年假有多少天”，第二轮接着问“怎么申请”。这个怎么申请单独拿去检索，系统根本不知道在问啥。得把上文的年假补进去，变成年假怎么申请，才能检索到正确的内容。
- **多意图**：“请假流程是什么，另外帮我查下我还剩几天年假”——这其实是两个问题，一个要检索知识库，一个要查业务数据。你得先把它拆开，分别处理。
- **复杂问题**：用户问“我想申请调岗到上海，需要满足什么条件，流程是什么，大概要多久”——这个问题涉及多个方面，可能散落在不同的文档里。直接检索未必能覆盖全，拆成几个子问题分别检索，再合并答案，效果会更好。

问题重写这一步，本质上是在弥补用户表达和检索需要之间的 gap。做不做、做多深，直接影响后面的检索质量。

### 3. 意图识别

用户发来一句话，系统得先判断：这句话想干嘛？不是所有问题都需要走 RAG 流程。

- **闲聊**：用户说“你是ChatGPT么？”，这种情况去知识库里检索，能检索出什么？这种情况应该直接让模型回复，不用走检索。
- **工具调用**：“报销制度是什么”需要检索知识库，“我这个月的报销单到哪一步了”需要调用业务系统接口。走错了路，答案肯定不对。
- **多知识库路由**：企业里可能有多个知识库——HR 政策库、产品文档库、技术文档库。用户问的问题应该去哪个库里找？如果每个库都搜一遍，成本高、噪音大；如果只搜一个库，选错了就什么都检索不到。
- **该不该回答**：有些问题不该回答。涉及敏感信息的、超出系统能力范围的、恶意试探的，都需要识别出来，给出合适的拒绝或引导。

意图识别做得不好，后面的流程再完美也没用——方向错了，跑得越快错得越远。

### 4. 数据检索

这是 RAG 的核心环节，也是最容易出问题的环节。

- **向量检索的局限**：纯向量检索对语义相似性很敏感，但对精确匹配很弱。用户问“BRD-2024-0312 这个需求的状态”，这是个精确的编号，向量检索可能完全找不到。类似的还有人名、产品型号、订单号这些，光靠向量不够。
- **混合检索的取舍**：为了弥补上面这个问题，通常会把向量检索和关键词检索结合起来。但两边的结果怎么融合？权重怎么设？不同场景可能需要不同的策略。
- **top-k 选多少**：检索返回多少条结果？选少了可能漏掉关键信息，选多了塞给模型一大堆内容，它可能反而被干扰。而且返回越多，后续处理的成本也越高。
- **结果重排序**：向量相似度高的，不一定是最相关的。很多时候需要再加一层重排序（Reranking），用更精细的模型对检索结果重新打分排序。这一步能明显提升最终效果，但也增加了延迟和成本。
- **召回了但不该用**：检索到的内容不一定都该用。有些可能是过时的版本，有些可能相关度其实不够，有些可能用户没有权限看。在塞给模型之前，还需要做一轮过滤和筛选。

### 5. 会话记忆

单轮问答相对简单，多轮对话的复杂度会陡增。

- **上下文要带多少**：用户聊了 20 轮，你把所有历史对话都塞给模型？Token 成本扛不住，模型也容易被早期不相关的内容干扰。但如果只带最近几轮，又可能丢失重要的上下文信息。
- **记忆的压缩和摘要**：一种做法是对历史对话做摘要，提取关键信息，用摘要代替完整的历史记录。但摘要过程本身可能丢失细节，摘错了后面就全错了。
- **长期记忆**：有些场景需要跨会话的记忆。用户上周问过的问题，这周再来的时候系统还能记得。这涉及到记忆的持久化存储和检索，又是一套单独的机制。
- **记忆的更新和遗忘**：用户在对话中纠正了之前的信息，系统得能更新记忆。有些过时的信息需要遗忘，不能一直带着。这些都需要额外的逻辑来处理。

### 6. 其他功能

除了主流程，一个完整的 RAG 系统还需要处理很多边边角角的事情。

**引导澄清**

用户的问题太模糊，没法检索，也没法回答。比如用户就说一个“报销”，系统应该反问：你是想了解报销流程，还是查询报销单状态，还是想提交新的报销？这需要系统能识别出问题不够明确，并给出合适的引导。

**请求风控**

总有人会试图让系统说一些不该说的话，或者套取敏感信息。需要在入口处做一层过滤，识别并拦截恶意请求。同时如果知识库资料包含公司隐私数据，需要部署本地大模型，对应的限流风控也需要做。

**停止生成**

用户发现答案不对，想中断重新问，系统得能响应停止指令，及时终止生成，不要傻傻地继续输出。

**模型负载均衡**

线上流量大的时候，单个模型实例扛不住。需要有负载均衡机制，多个模型实例之间分配请求。如果用的是第三方 API，还要处理限流、降级、多供应商切换等问题。

**答案溯源**

用户看到一个答案，想知道依据是什么。系统需要能标注出答案来自哪个文档的哪个部分，让用户可以点进去查看原文。这对于建立信任、方便核实很重要。

**效果监控和反馈收集**

系统上线后，怎么知道答得好不好？需要有机制收集用户的反馈（点赞、点踩、纠正），监控各个环节的指标，为后续优化提供数据支撑。

------

以上这些，每个点单独拎出来都不算特别难。但当它们组合在一起，形成一个完整的 RAG 系统时，复杂度是指数级上升的。

> 这里总结的是高频难点清单，但并不意味着问题已穷尽。针对具体场景，持续补充解决策略。



### 7. 参考技术栈

- 文档解析工具(后续增强,v1 不需要):https://github.com/apache/tika，本地用docker起HTTP服务，端口为`9998`。v1 仅限 markdown,直接读纯文本即可

